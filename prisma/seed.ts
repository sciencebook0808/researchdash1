/**
 * Seed file — intentionally empty.
 * 
 * Prausdit Research Lab v0.3+ uses manual data creation through the UI.
 * All data (documents, experiments, datasets, notes, roadmap steps)
 * must be created through the dashboard forms or the Gemini AI assistant.
 * 
 * To create your first content:
 * 1. Start the dev server: npm run dev
 * 2. Sign in via Clerk
 * 3. Navigate to any section and click the "+ New" button
 * 4. Or use the AI assistant with /document, /experiment, etc.
 */

console.log("No seed data — all content is created through the Prausdit Research Lab UI.")
